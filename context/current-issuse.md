Resolve these UI Related minor issuses:

- Center the Editor home `Heading, Description, and Create Project button`.
- Also change the cursor to pointer on hover over buttons everywhere.

Create `Hook` folder in root and move `use-project-dialogs.ts` to it and for future add all hook related files to this folder.