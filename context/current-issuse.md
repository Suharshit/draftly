Read all below given issuse and Resolve them with following Rules and Architecture.

1. In Canvas UI i want to change the apperance of shapes.
  - Circle should be in circle shape.
  - Rectangle should be in rectangle shape.
  - Diamond should be in diamond shape.
  - Pill should be in pill shape.
  - Cylinder should be in cylinder shape.
  - Hexagon should be in hexagon shape.

2. The connection should be in curvey lines from source node handle to target node handle.

3. When i try connecting two nodes, i dragged the mouse from source node top to target node bottom but it connect source node bottom to target node top. fix this issue.

4. Make default size of shapes little smaller and can be changed using edge drag and by typing width and height on edge in (pixels) and after edit should go hidden and only appers on node hover.