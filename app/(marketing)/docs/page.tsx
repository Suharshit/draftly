import { DraftlyLoader } from "@/components/ui/marketing"

// Temporary: docs has no content yet, so it previews the loading screen.
export default function DocsPage() {
  return (
    <main>
      <DraftlyLoader />
    </main>
  )
}
